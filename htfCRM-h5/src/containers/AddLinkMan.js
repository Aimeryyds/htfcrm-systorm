import {
    connect
} from 'react-redux'

import AddLinkMan from '../components/linkMan/addLinkMan';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(AddLinkMan);