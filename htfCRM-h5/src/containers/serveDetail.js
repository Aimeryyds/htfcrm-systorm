import {
    connect
} from 'react-redux'

import ServeDetail from '../components/serve/detail';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(ServeDetail);