import {
    connect
} from 'react-redux'

import ManagerInfo from '../components/managerFile/personInfo';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(ManagerInfo);