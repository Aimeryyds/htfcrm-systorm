import {
    connect
} from 'react-redux'

import TradingDetali from '../components/view360/trading_detail';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(TradingDetali);