import {
    connect
} from 'react-redux'

import ManagerDetail from '../components/managerFile/detail';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(ManagerDetail);