import {
    connect
} from 'react-redux'

import ServeList from '../components/serve';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(ServeList);