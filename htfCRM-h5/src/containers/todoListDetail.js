import {
	connect
} from 'react-redux'

import TodoListDetail from '../components/todoList/detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(TodoListDetail);