import {
	connect
} from 'react-redux'

import JZListDetailPro from '../components/JZList/detail_pro';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(JZListDetailPro);