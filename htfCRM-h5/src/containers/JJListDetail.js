import {
	connect
} from 'react-redux'

import JJListDetail from '../components/JJList/detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(JJListDetail);