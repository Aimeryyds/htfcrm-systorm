import {
    connect
} from 'react-redux'

import LevelAnalyze from '../components/levelAnalyze';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(LevelAnalyze);