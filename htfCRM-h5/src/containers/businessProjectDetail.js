import {
    connect
} from 'react-redux'

import BusinessProjectDetail from '../components/businessProject/detail';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(BusinessProjectDetail);