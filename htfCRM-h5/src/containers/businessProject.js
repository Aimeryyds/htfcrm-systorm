import {
    connect
} from 'react-redux'

import BusinessProject from '../components/businessProject';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(BusinessProject);