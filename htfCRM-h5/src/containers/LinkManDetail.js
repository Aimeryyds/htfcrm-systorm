import {
	connect
} from 'react-redux'

import LinkManDetail from '../components/linkMan/detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(LinkManDetail);