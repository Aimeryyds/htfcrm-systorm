import {
	connect
} from 'react-redux'

import SKListDetail from '../components/SKList/detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(SKListDetail);