import {
    connect
} from 'react-redux'

import BusinessProjectList from '../components/businessProject/list';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(BusinessProjectList);