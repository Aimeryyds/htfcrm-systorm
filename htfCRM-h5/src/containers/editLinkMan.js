import {
    connect
} from 'react-redux'

import EditLinkMan from '../components/linkMan/editLinkMan';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(EditLinkMan);