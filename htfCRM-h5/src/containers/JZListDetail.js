import {
	connect
} from 'react-redux'

import JZListDetail from '../components/JZList/detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(JZListDetail);