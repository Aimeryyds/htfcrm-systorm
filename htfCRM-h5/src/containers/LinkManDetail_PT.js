import {
	connect
} from 'react-redux'

import LinkManDetail_PT from '../components/linkMan_PT/detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(LinkManDetail_PT);