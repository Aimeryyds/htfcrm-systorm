import {
	connect
} from 'react-redux'

import SKEditDetail from '../components/SKList/edit_detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(SKEditDetail);