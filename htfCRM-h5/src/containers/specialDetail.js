import {
	connect
} from 'react-redux'

import SpecialDetail from '../components/specialSales/detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(SpecialDetail);