import {
    connect
} from 'react-redux'

import ManagerFile from '../components/managerFile';

const mapStateToProps = state => {
    return {
        
    }
};
export default connect(mapStateToProps)(ManagerFile);