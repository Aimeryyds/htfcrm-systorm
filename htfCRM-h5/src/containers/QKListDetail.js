import {
	connect
} from 'react-redux'

import QKListDetail from '../components/QKList/detail';

const mapStateToProps = state => {
	return {

	}
};
export default connect(mapStateToProps)(QKListDetail);